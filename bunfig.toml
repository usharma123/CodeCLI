[install]
peer = false

preload = ["@opentui/solid/preload"]

# JSX configuration - use React for src/ui/ (legacy) and Solid for src/tui/
# Since we can't have per-path JSX config, we default to React and use a workaround
